!function (d) {
    var f = "cookie_prompt";
    var e = 3650;
    var a = 20000;
    var b = function () {
        if (e === -1) {
            return false
        }
        var g = document.cookie.split("; ");
        var h = null;
        d(g).each(function (j, k) {
            var i = k.split("=");
            if (i[0] === f) {
                h = i[1]
            }
        });
        return h
    };
    var c = function () {
        var g;
        if (e === -1) {
            return
        }
        var h = f + "=1";
        if (e > 0) {
            g = new Date();
            g.setTime(g.getTime() + (e * 24 * 60 * 60 * 1000));
            h += "; expires=" + g.toGMTString()
        }
        document.cookie = h
    };
    d.fn.cookiesmodal = function () {
        var h;
        var g = this;
        if (!b()) {
            c();
            g.fadeIn("fast");
            if (a) {
                h = window.setTimeout(function () {
                    g.fadeOut("fast")
                }, a)
            }
            g.find('[data-dismiss="modal"]').click(function (i) {
                i.preventDefault();
                g.fadeOut("fast");
                window.clearTimeout(h)
            })
        }
    }
}(window.jQuery);